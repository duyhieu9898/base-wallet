export enum ChainId {
  MAINNET = 56,
  KOVAN = 97, // TESTNET
  ETH_MAINNET = 1,
}

export const NetworkContextName = 'NETWORK';

export enum SORTER_DIRECTIONS {
  ASCEND = 'ascend',
  DESCEND = 'descend',
}
